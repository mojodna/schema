[project]
name = "overture-schema-addresses-theme"
dynamic = ["version"]
description = "Overture Maps addresses theme models and structures"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
dependencies = [
    "overture-schema-core",
    "pydantic>=2.0",
]

[tool.uv.sources]
overture-schema-core = { workspace = true }


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "src/overture/schema/addresses/__about__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/overture"]

[dependency-groups]
dev = [
    "pytest>=7.0",
    "ruff",
    "mypy",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[project.entry-points."overture.models"]
"addresses.address" = "overture.schema.addresses.address.models:Address"

# duckdb -json -c "load spatial" -c "select to_json(columns(*)) from (select * REPLACE ST_AsText(geometry) as geometry from read_parquet('geoparquet-samples/address.geoparquet') USING SAMPLE 1)" | jq .
[[examples.Address]]
id = "416ab01c-d836-4c4f-aedc-2f30941ce94d"
geometry = "POINT (-176.5637854 -43.9471955)"
country = "NZ"
postcode = "null"
street = "Tikitiki Hill Road"
number = "54"
unit = "null"
postal_city = "null"
version = 1
theme = "addresses"
type = "address"

[examples.Address.bbox]
xmin = -176.56381225585938
xmax = -176.56378173828125
ymin = -43.94719696044922
ymax = -43.94718933105469

[[examples.Address.address_levels]]
value = "Chatham Islands"

[[examples.Address.address_levels]]
value = "Chatham Island"

[[examples.Address.sources]]
property = ""
dataset = "OpenAddresses/LINZ"
record_id = "null"
update_time = "null"
confidence = "null"
between = "null"
