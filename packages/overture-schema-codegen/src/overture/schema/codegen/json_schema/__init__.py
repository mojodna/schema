"""JSON Schema generation from the codegen extraction IR.

Spike-quality (see `spike/RENDERER_SPEC.md`): produces a divergence census
against Pydantic's `model_json_schema()` baselines, not a shippable renderer.
"""
