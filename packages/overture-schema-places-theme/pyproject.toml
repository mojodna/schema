[project]
name = "overture-schema-places-theme"
dynamic = ["version"]
description = "Overture Maps places theme with place type models"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
dependencies = [
    "overture-schema-core",
    "overture-schema-validation",
    "pydantic>=2.0",
    "pydantic[email]",
]

[tool.uv.sources]
overture-schema-core = { workspace = true }
overture-schema-validation = { workspace = true }

[dependency-groups]
dev = [
    "pytest>=7.0",
    "ruff",
    "mypy",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "src/overture/schema/places/__about__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/overture"]

[project.entry-points."overture.models"]
"places.place" = "overture.schema.places.place.models:Place"

# duckdb -json -c "load spatial" -c "select to_json(columns(*)) from (select * REPLACE ST_AsText(geometry) as geometry from read_parquet('geoparquet-samples/place.geoparquet') USING SAMPLE 1)" | jq .
[[examples.Place]]
id = "25d1faae-e887-493b-97d6-1c5bf39ed51f"
geometry = "POINT (-151.7990018 -79.5664328)"
version = 1
confidence = 0.8015267175572519
websites = [
  "https://www.primenewsmonde.bj/"
]
socials = [
  "https://www.facebook.com/105157324629740"
]
emails = "null"
phones = [
  "+22991510404"
]
theme = "places"
type = "place"

[examples.Place.bbox]
xmin = -151.7990264892578
xmax = -151.7989959716797
ymin = -79.56644439697266
ymax = -79.5664291381836

[[examples.Place.sources]]
property = ""
dataset = "meta"
record_id = "105157324629740"
update_time = "2025-06-30T07:00:00.000Z"
confidence = 0.8015267175572519
between = "null"

[examples.Place.names]
primary = "Prime News TV Monde"
common = "null"
rules = "null"

[examples.Place.categories]
primary = "media_news_website"
alternate = "null"

[examples.Place.brand]
wikidata = "null"

[examples.Place.brand.names]
primary = "null"
common = "null"
rules = "null"

[[examples.Place.addresses]]
freeform = "Fidjrossè Calvaire Villa située entre Sun Beach Hôtel et l'agence PDME. "
locality = "Fidjrossè-Centre"
postcode = "null"
region = "null"
country = "BJ"
